#!/usr/bin/env bash
set -euo pipefail
if [ -f "$HOME/rover/sim.pid" ]; then
  PID=$(cat "$HOME/rover/sim.pid")
  kill "$PID" 2>/dev/null || true
  rm -f "$HOME/rover/sim.pid"
fi
rm -f "$HOME/rover/sim_port"
echo "simulator stopped"
